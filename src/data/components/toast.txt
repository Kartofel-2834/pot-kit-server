<script lang="ts" generic="DATA = any" setup>
// Types
import type { I<%camel%>ToastExpose, I<%camel%>ToastProps } from '<%typesImport%>/components/toast';
import type { IToastDialog } from '<%typesImport%>/composables/toast';

// Vue
import { computed, toRef } from 'vue';

// Constants
import { <%upper%>_TOAST_POSITION } from '<%typesImport%>/components/toast';
import { DIALOG_LAYERS } from '<%typesImport%>/composables/dialog';

// Composables
import { useToast } from '<%composablesImport%>/toast';
import { useDialog } from '<%composablesImport%>/dialog';
import { useDeviceProperties } from '<%composablesImport%>/device-is';
import { useClassList } from '<%composablesImport%>/class-list';

const $props = withDefaults(defineProps<I<%camel%>ToastProps<DATA>>(), {
    group: undefined,
    position: <%upper%>_TOAST_POSITION.TOP_LEFT,
    to: 'body',
    transition: '<%kebab%>-toast-transition',
});

// Computed
const toastsList = computed(() => {
    return $toast.list.value.filter(toast => toast.group === $props.group) as IToastDialog<DATA>[];
});

const teleportTo = computed(() => $props.to ?? 'body');

const currentStyles = computed(() => ({ zIndex: $dialog.zIndex.value }));

// Composables
const $toast = useToast<DATA>();

const $dialog = useDialog({
    triggers: [],
    isOpen: toRef(() => Boolean($toast.list.value.length)),
    layer: DIALOG_LAYERS.TOAST,
    close: pop,
    open: () => {},
});

const $properties = useDeviceProperties(
    {
        position: toRef(() => $props.position),
        size: toRef(() => $props.size),
        color: toRef(() => $props.color),
        radius: toRef(() => $props.radius),
    },
    toRef(() => $props.devices),
);

const $classList = useClassList(
    {
        position: $properties.position,
        size: $properties.size,
        color: $properties.color,
        radius: $properties.radius,
    },
    'toast',
);

// Methods
function getData(toast: IToastDialog): DATA {
    return toast.data as DATA;
}

function pop() {
    const firstToast = $toast.list.value[0];
    if (firstToast) close(firstToast.id);
}

function close(id: Symbol) {
    $toast.remove(id);
}

// Exports
defineExpose<I<%camel%>ToastExpose<DATA>>({
    list: toastsList,
    pop,
    close,
});
</script>

<template>
    <Teleport
        :to="teleportTo"
        :disabled="!to"
    >
        <TransitionGroup
            :name="transition"
            :class="['<%kebab%>-toast', $classList, classList]"
            :style="currentStyles"
            v-bind="$dialog.marker"
            tag="div"
            role="alert"
        >
            <div
                v-for="(toastDialog, index) in toastsList"
                :key="toastDialog.id.description"
                ref="toastDialogsList"
                class="<%kebab%>-toast-item"
            >
                <div class="<%kebab%>-toast-item-container">
                    <slot
                        :id="toastDialog.id"
                        :data="getData(toastDialog)"
                        :index="index"
                        :close="() => close(toastDialog.id)"
                    >
                        {{ toastDialog.data }}
                    </slot>
                </div>
            </div>
        </TransitionGroup>
    </Teleport>
</template>

<style>
.<%kebab%>-toast {
    position: fixed;
    display: flex;
    flex-direction: column;
}

.<%kebab%>-toast-item {
    position: relative;
    max-height: 1000px;
}

.<%kebab%>-toast-item-container {
    border-style: solid;

    /* --- <%camel%>Toast - Color --- */
    border-color: var(--<%kebab%>-toast-color-border, transparent);
    background-color: var(--<%kebab%>-toast-color-background, transparent);
    color: var(--<%kebab%>-toast-color-text, inherit);

    /* --- <%camel%>Toast - Size --- */
    border-width: var(--<%kebab%>-toast-size-border, 0);
    padding: var(--<%kebab%>-toast-size-padding, 0);
    box-shadow: var(--<%kebab%>-toast-size-shadow, none);
    font-size: var(--<%kebab%>-toast-size-text, inherit);

    /* --- <%camel%>Toast - Radius --- */
    border-radius: var(--<%kebab%>-toast-radius-value, 0);
}

/* --- Position - Top --- */
._toast-position-top-left .<%kebab%>-toast-item-container,
._toast-position-top-center .<%kebab%>-toast-item-container,
._toast-position-top-right .<%kebab%>-toast-item-container {
    margin-bottom: var(--<%kebab%>-toast-size-gap, 20px);
}

/* --- Position - Bottom --- */
._toast-position-bottom-left .<%kebab%>-toast-item-container,
._toast-position-bottom-center .<%kebab%>-toast-item-container,
._toast-position-bottom-right .<%kebab%>-toast-item-container {
    margin-top: var(--<%kebab%>-toast-size-gap, 20px);
}

/* --- Position - Top-left  --- */
._toast-position-top-left {
    top: var(--<%kebab%>-toast-size-edge-margin, 20px);
    left: var(--<%kebab%>-toast-size-edge-margin, 20px);
}

/* --- Position - Top-center  --- */
._toast-position-top-center {
    top: var(--<%kebab%>-toast-size-edge-margin, 20px);
    left: 50%;
    transform: translateX(-50%);
}

/* --- Position - Top-right  --- */
._toast-position-top-right {
    top: var(--<%kebab%>-toast-size-edge-margin, 20px);
    right: var(--<%kebab%>-toast-size-edge-margin, 20px);
}

/* --- Position - Bottom-left  --- */
._toast-position-bottom-left {
    flex-direction: column-reverse;
    bottom: var(--<%kebab%>-toast-size-edge-margin, 20px);
    left: var(--<%kebab%>-toast-size-edge-margin, 20px);
}

/* --- Position - Bottom-center  --- */
._toast-position-bottom-center {
    flex-direction: column-reverse;
    bottom: var(--<%kebab%>-toast-size-edge-margin, 20px);
    left: 50%;
    transform: translateX(-50%);
}

/* --- Position - Bottom-right  --- */
._toast-position-bottom-right {
    flex-direction: column-reverse;
    bottom: var(--<%kebab%>-toast-size-edge-margin, 20px);
    right: var(--<%kebab%>-toast-size-edge-margin, 20px);
}

/* --- Transition --- */
.<%kebab%>-toast-transition-enter-active,
.<%kebab%>-toast-transition-leave-active {
    transition:
        max-height 0.45s cubic-bezier(0, 1, 0, 1),
        opacity 0.2s,
        margin-bottom 0.4s,
        margin-top 0.4s;
}

.<%kebab%>-toast-transition-enter-from,
.<%kebab%>-toast-transition-leave-to {
    opacity: 0;
    margin-bottom: 0;
    margin-top: 0;
    max-height: 0;
}
</style>