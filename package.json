{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "map-data": "bun run src/map.ts",
    "collect-dependencies": "bun run src/collect-dependencies.ts",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@elysiajs/static": "^1.2.0",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "^3.5.3",
    "wrangler": "^4.12.0"
  },
  "module": "src/index.js"
}